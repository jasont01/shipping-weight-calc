export enum Panel {
  MX = 0,
  HC = 1,
  MXi = 2,
  XU = 3,
}

export enum Cabinet {
  Large = 0,
  Small = 1,
  Mini = 2,
}

export enum Config {
  Kiosk = 0,
  Addon = 1,
}

export enum Mount {
  Wall = 0,
  Stand = 1,
  None = 2,
}

export enum Accessory {
  APC = 0,
  Shelf = 1,
  Zebra = 2,
  Puck = 3,
}
