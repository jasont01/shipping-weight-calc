export enum Tab {
  Cabinets = 0,
  Hybrids = 1,
  DealerPlate = 2,
  Mini = 3,
  Accessories = 4,
  Mechanical = 5,
}

export enum Panel {
  MX = 0,
  HC = 1,
  MXi = 2,
  XU = 3,
  DP = 4,
}

export enum Cab {
  Large = 0,
  Small = 1,
  Mini = 2,
}

export enum Config {
  Kiosk = 0,
  Addon = 1,
}

export enum Mount {
  Wall = 0,
  Stand = 1,
  None = 2,
}

export enum Accessory {
  APC = 0,
  Shelf = 1,
  Zebra = 2,
  Puck = 3,
}

export enum Hybrid {
  DP = 0,
  Xi = 1,
}
